defmodule ExampleNumbers do

  def numbers do
    a = 10
    # b = 3.0
    # b = 3
    a = a + 5

    # IO.puts(a + b)
    IO.puts(a)
    IO.puts(Float.ceil(0.5))
    IO.puts(Integer.gcd(10, 25))
  end

end
